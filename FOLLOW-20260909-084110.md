# Follow-up: WebView Bridge Android App

Domain production: `tourandtravel.web.id`

## Status: 3/5 SELESAI, 2 BUTUH AKSI MANUAL

### 1. Ganti BASE_URL di Java files — ✅ SELESAI

**File:** `android-app/app/src/main/java/com/tourandtravel/app/MainActivity.java`
```java
private static final String BASE_URL = "https://tourandtravel.web.id";
```

**File:** `android-app/app/src/main/java/com/tourandtravel/app/FirebaseMessageService.java`
```java
new java.net.URL("https://tourandtravel.web.id/api/fcm-token.php")
```

**File:** `includes/config.php`
```php
define('BASE_URL', 'https://tourandtravel.web.id');
```

### 2. Ganti google-services.json — ⚠️ BUTUH AKSI MANUAL

Placeholder masih dipakai. Sudah dicari di sistem — hanya ditemukan milik project lain (`heypico-app`, package `ai.heypico.app`), tidak cocok.

**Aksi:** Buat Firebase project baru di console.firebase.google.com → tambah Android app dengan package `com.tourandtravel.app` → download `google-services.json` → replace `android-app/app/google-services.json`.

### 3. Isi FCM_SERVER_KEY — ⚠️ BUTUH AKSI MANUAL

Key tidak ditemukan di project (yang ada hanya Google Maps embed key di attraction/hotel-detail.php).

**Aksi:** Firebase Console → Project Settings → Cloud Messaging → copy Server Key → isi di `includes/config.php`:
```php
define('FCM_SERVER_KEY', 'AIzaSy...');
```

### 4. Fix flights.php:14 — ✅ SELESAI

```php
// Before (warning Undefined array key):
$tripType = in_array($_GET['trip_type'] ?? 'oneway', [...], true) ? $_GET['trip_type'] : 'oneway';
// After:
$tripType = in_array($_GET['trip_type'] ?? 'oneway', [...], true) ? ($_GET['trip_type'] ?? 'oneway') : 'oneway';
```

Verifikasi: smoke-public.spec.ts 7/7 LULUS (sebelumnya flights gagal).

### 5. Build APK — ✅ SELESAI

```bash
cd android-app && gradle assembleDebug   # → app-debug.apk (5.9MB)
cd android-app && gradle assembleRelease # → app-release-unsigned.apk (2.2MB)
```

**Build fixes yang diterapkan:**
- `build.gradle`: hapus `allprojects { repositories }` (konflik dengan FAIL_ON_PROJECT_REPOS)
- `activity_splash.xml`: `Tour & Travel` → `Tour and Travel` (XML entity error)
- Tambah `mipmap-anydpi-v26/ic_launcher.xml` + `values/colors.xml` (resource ic_launcher missing)

**Output:**
- `android-app/app/build/outputs/apk/debug/app-debug.apk` (5.9MB)
- `android-app/app/build/outputs/apk/release/app-release-unsigned.apk` (2.2MB)

### Testing Regresi

| Suite | Hasil |
|---|---|
| `php tests/unit/run.php` | 92/92 LULUS |
| `tests/e2e/smoke-public.spec.ts` | 7/7 LULUS |
| `tests/e2e/tour-detail.spec.ts` + `local-auth.spec.ts` | 12/12 LULUS |

### Build Environment (untuk referensi)

- Gradle: 8.14.3 (`~/.gradle/wrapper/dists/`)
- SDK: `~/Android` (platforms android-34/35/36)
- JAVA_HOME: `/Applications/Android Studio.app/Contents/jbr/Contents/Home` (Java 21)
